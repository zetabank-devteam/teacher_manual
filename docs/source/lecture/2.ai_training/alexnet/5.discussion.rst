Discussion
=============

Both GoogleNet and AlexNet image recognition system can identify an object, but they cannot identify multiple objects.

.. raw:: html

    <div style="background: #ffe5b4" class="admonition note custom">
        <p style="background: #ffbf00" class="admonition-title">
            Group Discussion Points
        </p>
        <div class="line-block">
            <div class="line"><strong>-</strong> How can image recognition system be improved so that it can detect multiple objects?</div>
            <div class="line"><strong>-</strong> Why would we need to detect multiple objects within a screen? Where would this system be implemented?</div>
        </div>
    </div>
